/**
 * Common Module - Exports
 * 
 * Shared utilities used across all microservices:
 * - Decorators: @CurrentUser(), @Roles()
 * - Guards: RolesGuard
 * - Pipes: (future)
 * - Interceptors: (future)
 * - Filters: (future)
 */

export * from './decorators';
export * from './guards';
